import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Renzo | Software Engineer",
  description:
    "Portfolio pessoal — Software Engineer com foco em sistemas distribuídos, cloud e developer experience.",
  keywords: ["software engineer", "portfolio", "full-stack", "cloud", "typescript"],
  authors: [{ name: "Renzo" }],
  openGraph: {
    title: "Renzo | Software Engineer",
    description: "Portfolio pessoal e projetos.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="pt-BR"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
